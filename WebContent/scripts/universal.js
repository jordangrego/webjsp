$(function() {
	/*$(".senha")
	.blur(
			function() {
				var confirmacaoSenha = $(this).val();
				var senha = $("#txtSenha").val();
				$(this).next().remove();
				if (senha !== confirmacaoSenha) {

					if ($(this).next().html === "As senhas não coincidem") {

					} else {
						$(this)
								.after(
										"<span class='help-block'>As senhas não coincidem</span>");

						adicionaClass($(this).parent(), 'has-error');
					}

				} else {
					$(this).next().next().remove();
					$(this).parent().removeClass("has-error");
					$(this).parent().parent().removeClass("has-error");

				}

			});*/
});